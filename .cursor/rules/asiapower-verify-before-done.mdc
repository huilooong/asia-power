---
description: AsiaPower verification gate — prove it works before saying done
alwaysApply: true
---

# 完成前验证（轻量版，不是 80% 覆盖强迫症）

改编自 ECC testing/workflow：保留「做完要证明」，去掉「一切强制 TDD 80%」。

## 什么叫「做完」

必须同时满足：

1. **有交付物路径**（预览 / 报告 / 代码文件）
2. **有验证证据**（命令输出、URL 状态、截图、队列状态之一）
3. **用表格汇报**：成功 / 失败 / 下一步

## 按任务类型最低验证

| 任务类型 | 最低验证 |
|----------|----------|
| 公开页 / UI | 本地或预览 URL 可打开；CEO 未批不得上生产 |
| API / 库存逻辑 | 用真实或快照数据跑通关键路径；写明影响库存号 |
| QXB 上传 | 看队列/`reports` 状态；VIN/429 先查 runbook |
| 生产部署 | Release ID + 备份 + 健康检查 + 可回滚说明 |
| 销售数字 | `python -m truth.verified_sales_intelligence`，禁止瞎猜 |

## 禁止

- 只改代码不验证就说「已修复」
- 用 `--no-verify` 跳过 git hook（除非 CEO 明确要求）
- 为了「让测试绿」去改坏断言而不修实现（测试错了再改测试，并说明原因）

## 小改动可以简化，但不能为零

一行文案 / 拼写：打开相关页或预览确认即可。  
涉及价格、状态、上传、权限：必须留验证证据。