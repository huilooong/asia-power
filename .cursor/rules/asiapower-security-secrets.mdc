---
description: AsiaPower secret & safety checks — no leaked keys, no blind destructive ops
alwaysApply: true
---

# AsiaPower 安全与密钥

改编自 ECC security，收缩为公司实战红线。

## 提交 / 部署前必须核对

- [ ] 无硬编码密钥（`.env`、API Key、WhatsApp/企微/Resend、数据库口令）
- [ ] 新日志不打印 token、cookie、完整 VIN、客户手机号明文
- [ ] Admin / API 改动不误放开对公网
- [ ] 生产修改走 Release Manager（`scripts/deploy-production.mjs`），禁止手滑覆盖无备份

## 密钥管理

- 密钥只在 `.env` / 服务器环境变量；代码只读 `process.env` / dotenv
- 示例配置用 `.env.example` 占位符，永不提交真实值
- 疑似泄露：立刻停止写入记忆/报告明文 → 告知 CEO → 轮换密钥

## 破坏性操作（仍须先问 CEO）

- 删库存 / 删线索 / 批量改价
- `git push --force` 到 main
- 对外发邮件、社媒、广告文案自动群发
- 关闭限流或扩大 `TRUSTED_SUPPLIER_UPLOAD_IPS` 白名单

## 已授权 ≠ 无安全检查

即使 CEO 已授权生产 SSH / QXB 上传，仍须：

- 改完做现网或队列验证
- 失败先查根因（VIN / 429），不盲目重跑整批
- **生产部署硬红线（CEO 2026-07-10）**：必须 `commit → push GitHub → Release Manager`；禁止常态 `--allow-dirty` 直推；再犯 = 续费红线