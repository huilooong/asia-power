# Google Ads 新账号 — 564-118-0568

**日期：** 2026-07-19  
**来源：** CEO 确认已换新账号（旧半成品 `480-120-6293` 废弃）

| 项 | 值 |
|---|---|
| 账号 ID | **564-118-0568** |
| 网站转化容器 | **AW-5641180568** |
| 转化标签（generate_lead） | ⏳ 待建（`ADS_GENERATE_LEAD_LABEL` 仍为空） |
| 关键词 CSV 包 | 已备好：`docs/marketing/google-ads-merged-africa-editor-2026-07-06/`（Paused） |

## 已做

- 网站 `js/main.js`、`js/half-cut-leads.js` 的 `ADS_CONVERSION_ID` 已从 `AW-4801206293` 改为 `AW-5641180568`

## CEO 下一步（约 5 分钟）

1. Ads → **目标 → 转化 → 新建转化操作** → 网站 → 潜在客户 / 表单提交一类
2. 复制转化标签里 `/` 后面那段（例如 `AbCdEfGhIjKlMnOp`）
3. 发给我 → 写入 `ADS_GENERATE_LEAD_LABEL` 并部署
4. 结算页确认已绑卡、无「结算不支持」
5. 账号正常后用 Ads Editor 导入 CSV（全部 Paused，不扣费）；启用时先只开「发动机」系列试跑

## 旧账号

`480-120-6293` 仅作历史记录，勿再使用。
